{
  "dependencies": {
    "pino": "^10.3.1"
  },
  "description": "Shared observability primitives for ChronoQueue: service-scoped structured logging, dependency-free Prometheus-format metrics, safe error serialization, and canonical lifecycle event names.",
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  },
  "main": "./dist/index.js",
  "name": "@chronoqueue/observability",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "types": "./dist/index.d.ts",
  "version": "1.0.0"
}